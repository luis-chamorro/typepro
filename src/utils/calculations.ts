/**
 * Game utility functions
 * Reserved for future score calculations and game mechanics
 */

// Placeholder for future upgrade system utilities
export const isVowel = (char: string): boolean => {
  return 'aeiouAEIOU'.includes(char);
};
